class Painting {
  name;
  canvasSize;
  price;

  constructor(name, canvasSize, price) {
    this.name = name;
    this.canvasSize = canvasSize;
    this.price = price;
  }
}

export default Painting;
